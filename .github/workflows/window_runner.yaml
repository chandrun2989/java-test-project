# Custom scan log filtering script

# Previous step assumes output is captured in a variable called 'defender_logs'

# Filter the logs to show only custom scan details
custom_scan_logs=$(echo "$defender_logs" | grep 'custom scan detail')

echo "$custom_scan_logs"